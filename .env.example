# Example environment variables for local development
# Copy this file to .env and fill in real values as needed.

# Vite frontend API base URL. Use empty string to call relative paths (e.g., when proxying through dev server).
VITE_API_URL=http://localhost:3001

# Example backend env vars (if you run the example backend)
SUPABASE_URL=https://your-supabase-url.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
STRIPE_SECRET_KEY=sk_test_...

# Clerk / Auth keys (if required)
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Other recommended vars:
# VITE_SUPABASE_ANON_KEY=your-anon-key